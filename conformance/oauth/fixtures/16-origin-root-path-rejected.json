{
  "name": "origin-root-path-rejected",
  "description": "Resource origin carrying a path beyond \"/\" (or query/fragment/userinfo) violates the origin-root profile \u2192 usage error.",
  "operation": "discoverProtectedResource",
  "resourceOrigin": "https://api.example.com/tenant/1",
  "expect": {
    "outcome": "raise",
    "error": "usage",
    "launchpadContacted": false,
    "errorCategory": "usage"
  }
}
