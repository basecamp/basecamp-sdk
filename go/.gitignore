# Test artifacts
coverage.out
coverage.html

# Benchmark comparison files (generated, not committed)
bench-baseline.txt
bench-current.txt

# NOTE: pkg/basecamp/default.pgo is NOT ignored - PGO profiles should be committed
# to enable profile-guided optimization in builds (Go auto-detects in package dir)
