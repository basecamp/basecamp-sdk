// Generated by basecamp-sdk-generator from openapi.json and behavior-model.json. DO NOT EDIT.

//! Everything operations.

#![allow(
    clippy::too_many_arguments,
    clippy::wildcard_imports,
    clippy::doc_markdown
)]

use crate::client::AccountClient;
use crate::error::Error;
use crate::generated::routes;
#[allow(unused_imports)]
use crate::generated::types::*;
use crate::pagination::Page;

/// Optional query parameters for `GetEverythingCheckins`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetEverythingCheckinsParams {
    /// Page number for paginating through results. Defaults to 1. A positive value selects exactly that page, not a starting offset; see SPEC section 8.
    pub page: Option<i32>,
}

/// Optional query parameters for `GetEverythingComments`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetEverythingCommentsParams {
    /// Page number for paginating through results. Defaults to 1. A positive value selects exactly that page, not a starting offset; see SPEC section 8.
    pub page: Option<i32>,
}

/// Optional query parameters for `GetEverythingCompletedCards`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetEverythingCompletedCardsParams {
    /// Restrict to tasks assigned to at least one of the given people (repeatable).
    /// Assignees on nested steps are not considered.
    pub assignee_ids: Option<Vec<i64>>,
    /// Filter by due date: with, without, or overdue. Unrecognized values are ignored.
    pub due: Option<String>,
    /// Page number for paginating through results. Defaults to 1. A positive value selects exactly that page, not a starting offset; see SPEC section 8.
    pub page: Option<i32>,
}

/// Optional query parameters for `GetEverythingCompletedTodos`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetEverythingCompletedTodosParams {
    /// Restrict to tasks assigned to at least one of the given people (repeatable).
    /// Assignees on nested steps are not considered.
    pub assignee_ids: Option<Vec<i64>>,
    /// Filter by due date: with, without, or overdue. Unrecognized values are ignored.
    pub due: Option<String>,
    /// Page number for paginating through results. Defaults to 1. A positive value selects exactly that page, not a starting offset; see SPEC section 8.
    pub page: Option<i32>,
}

/// Optional query parameters for `GetEverythingFiles`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetEverythingFilesParams {
    /// Filter by file kind: all (default), images, pdfs, documents, or videos.
    pub kind: Option<String>,
    /// Restrict to files created by the given people (repeatable).
    pub people_ids: Option<Vec<i64>>,
    /// Page number for paginating through results. Defaults to 1. A positive value selects exactly that page, not a starting offset; see SPEC section 8.
    pub page: Option<i32>,
}

/// Optional query parameters for `GetEverythingForwards`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetEverythingForwardsParams {
    /// Page number for paginating through results. Defaults to 1. A positive value selects exactly that page, not a starting offset; see SPEC section 8.
    pub page: Option<i32>,
}

/// Optional query parameters for `GetEverythingMessages`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetEverythingMessagesParams {
    /// Page number for paginating through results. Defaults to 1. A positive value selects exactly that page, not a starting offset; see SPEC section 8.
    pub page: Option<i32>,
}

/// Optional query parameters for `GetEverythingNoDueDateCards`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetEverythingNoDueDateCardsParams {
    /// Restrict to tasks assigned to at least one of the given people (repeatable).
    /// Assignees on nested steps are not considered.
    pub assignee_ids: Option<Vec<i64>>,
    /// Filter by due date: with, without, or overdue. Unrecognized values are ignored.
    pub due: Option<String>,
    /// Page number for paginating through results. Defaults to 1. A positive value selects exactly that page, not a starting offset; see SPEC section 8.
    pub page: Option<i32>,
}

/// Optional query parameters for `GetEverythingNoDueDateTodos`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetEverythingNoDueDateTodosParams {
    /// Restrict to tasks assigned to at least one of the given people (repeatable).
    /// Assignees on nested steps are not considered.
    pub assignee_ids: Option<Vec<i64>>,
    /// Filter by due date: with, without, or overdue. Unrecognized values are ignored.
    pub due: Option<String>,
    /// Page number for paginating through results. Defaults to 1. A positive value selects exactly that page, not a starting offset; see SPEC section 8.
    pub page: Option<i32>,
}

/// Optional query parameters for `GetEverythingNotNowCards`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetEverythingNotNowCardsParams {
    /// Restrict to tasks assigned to at least one of the given people (repeatable).
    /// Assignees on nested steps are not considered.
    pub assignee_ids: Option<Vec<i64>>,
    /// Filter by due date: with, without, or overdue. Unrecognized values are ignored.
    pub due: Option<String>,
    /// Page number for paginating through results. Defaults to 1. A positive value selects exactly that page, not a starting offset; see SPEC section 8.
    pub page: Option<i32>,
}

/// Optional query parameters for `GetEverythingOpenCards`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetEverythingOpenCardsParams {
    /// Restrict to tasks assigned to at least one of the given people (repeatable).
    /// Assignees on nested steps are not considered.
    pub assignee_ids: Option<Vec<i64>>,
    /// Filter by due date: with, without, or overdue. Unrecognized values are ignored.
    pub due: Option<String>,
    /// Page number for paginating through results. Defaults to 1. A positive value selects exactly that page, not a starting offset; see SPEC section 8.
    pub page: Option<i32>,
}

/// Optional query parameters for `GetEverythingOpenTodos`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetEverythingOpenTodosParams {
    /// Restrict to tasks assigned to at least one of the given people (repeatable).
    /// Assignees on nested steps are not considered.
    pub assignee_ids: Option<Vec<i64>>,
    /// Filter by due date: with, without, or overdue. Unrecognized values are ignored.
    pub due: Option<String>,
    /// Page number for paginating through results. Defaults to 1. A positive value selects exactly that page, not a starting offset; see SPEC section 8.
    pub page: Option<i32>,
}

/// Optional query parameters for `GetEverythingOverdueCards`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetEverythingOverdueCardsParams {
    /// Restrict to tasks assigned to at least one of the given people (repeatable).
    /// Assignees on nested steps are not considered.
    pub assignee_ids: Option<Vec<i64>>,
    /// Filter by due date: with, without, or overdue. Unrecognized values are ignored.
    pub due: Option<String>,
}

/// Optional query parameters for `GetEverythingOverdueTodos`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetEverythingOverdueTodosParams {
    /// Restrict to tasks assigned to at least one of the given people (repeatable).
    /// Assignees on nested steps are not considered.
    pub assignee_ids: Option<Vec<i64>>,
    /// Filter by due date: with, without, or overdue. Unrecognized values are ignored.
    pub due: Option<String>,
}

/// Optional query parameters for `GetEverythingUnassignedCards`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetEverythingUnassignedCardsParams {
    /// Restrict to tasks assigned to at least one of the given people (repeatable).
    /// Assignees on nested steps are not considered.
    pub assignee_ids: Option<Vec<i64>>,
    /// Filter by due date: with, without, or overdue. Unrecognized values are ignored.
    pub due: Option<String>,
    /// Page number for paginating through results. Defaults to 1. A positive value selects exactly that page, not a starting offset; see SPEC section 8.
    pub page: Option<i32>,
}

/// Optional query parameters for `GetEverythingUnassignedTodos`.
#[derive(Debug, Clone, Default, PartialEq)]
pub struct GetEverythingUnassignedTodosParams {
    /// Restrict to tasks assigned to at least one of the given people (repeatable).
    /// Assignees on nested steps are not considered.
    pub assignee_ids: Option<Vec<i64>>,
    /// Filter by due date: with, without, or overdue. Unrecognized values are ignored.
    pub due: Option<String>,
    /// Page number for paginating through results. Defaults to 1. A positive value selects exactly that page, not a starting offset; see SPEC section 8.
    pub page: Option<i32>,
}

/// `Everything` operations, sent through one [`AccountClient`].
#[derive(Debug, Clone, Copy)]
pub struct EverythingService<'a> {
    client: &'a AccountClient,
}

impl<'a> EverythingService<'a> {
    pub(crate) fn new(client: &'a AccountClient) -> Self {
        Self { client }
    }

    /// The client this service sends through.
    pub fn client(&self) -> &'a AccountClient {
        self.client
    }

    /// Get every automatic check-in answer across all accessible projects, newest-first.
    /// Paginated; each item embeds its `bucket`.
    ///
    /// `GET /checkins.json` — idempotent; retries up to 3 attempt(s) on 429, 503.
    pub async fn everything_checkins(
        &self,
        params: &GetEverythingCheckinsParams,
    ) -> Result<Page<GetEverythingCheckinsResponseContent>, Error> {
        let mut operation = self.client.operation(&routes::GET_EVERYTHING_CHECKINS, &[]);
        operation.query_optional("page", params.page.as_ref());
        self.client.send_page(operation).await
    }

    /// Get every comment across all accessible projects, newest-first (paginated).
    /// Each item embeds its `bucket`.
    ///
    /// `GET /comments.json` — idempotent; retries up to 3 attempt(s) on 429, 503.
    pub async fn everything_comments(
        &self,
        params: &GetEverythingCommentsParams,
    ) -> Result<Page<GetEverythingCommentsResponseContent>, Error> {
        let mut operation = self.client.operation(&routes::GET_EVERYTHING_COMMENTS, &[]);
        operation.query_optional("page", params.page.as_ref());
        self.client.send_page(operation).await
    }

    /// Completed cards across all accessible projects, grouped by project (paginated).
    ///
    /// `GET /cards/completed.json` — idempotent; retries up to 3 attempt(s) on 429, 503.
    pub async fn everything_completed_cards(
        &self,
        params: &GetEverythingCompletedCardsParams,
    ) -> Result<Page<GetEverythingCompletedCardsResponseContent>, Error> {
        let mut operation = self
            .client
            .operation(&routes::GET_EVERYTHING_COMPLETED_CARDS, &[]);
        operation.query_list_optional("assignee_ids[]", params.assignee_ids.as_deref());
        operation.query_optional("due", params.due.as_ref());
        operation.query_optional("page", params.page.as_ref());
        self.client.send_page(operation).await
    }

    /// Completed to-dos across all accessible projects, grouped by project (paginated).
    ///
    /// `GET /todos/completed.json` — idempotent; retries up to 3 attempt(s) on 429, 503.
    pub async fn everything_completed_todos(
        &self,
        params: &GetEverythingCompletedTodosParams,
    ) -> Result<Page<GetEverythingCompletedTodosResponseContent>, Error> {
        let mut operation = self
            .client
            .operation(&routes::GET_EVERYTHING_COMPLETED_TODOS, &[]);
        operation.query_list_optional("assignee_ids[]", params.assignee_ids.as_deref());
        operation.query_optional("due", params.due.as_ref());
        operation.query_optional("page", params.page.as_ref());
        self.client.send_page(operation).await
    }

    /// Get every file recording across all accessible projects, newest-first (paginated).
    /// Heterogeneous: uploads and Basecamp documents carry their
    /// standard recording shapes, while rich-text attachments are wrapped in a
    /// recording envelope plus an `attachable_sgid` and blob metadata. Modeled as
    /// an optional-field superset (EverythingFile) so one element type decodes any
    /// variant.
    ///
    /// `GET /files.json` — idempotent; retries up to 3 attempt(s) on 429, 503.
    pub async fn everything_files(
        &self,
        params: &GetEverythingFilesParams,
    ) -> Result<Page<GetEverythingFilesResponseContent>, Error> {
        let mut operation = self.client.operation(&routes::GET_EVERYTHING_FILES, &[]);
        operation.query_optional("kind", params.kind.as_ref());
        operation.query_list_optional("people_ids[]", params.people_ids.as_deref());
        operation.query_optional("page", params.page.as_ref());
        self.client.send_page(operation).await
    }

    /// Get every inbox forward across all accessible projects, newest-first (paginated).
    /// Each item embeds its `bucket`.
    ///
    /// `GET /forwards.json` — idempotent; retries up to 3 attempt(s) on 429, 503.
    pub async fn everything_forwards(
        &self,
        params: &GetEverythingForwardsParams,
    ) -> Result<Page<GetEverythingForwardsResponseContent>, Error> {
        let mut operation = self.client.operation(&routes::GET_EVERYTHING_FORWARDS, &[]);
        operation.query_optional("page", params.page.as_ref());
        self.client.send_page(operation).await
    }

    /// Get every message across all accessible projects, newest-first (paginated).
    /// Each item embeds its `bucket` for project context.
    ///
    /// `GET /messages.json` — idempotent; retries up to 3 attempt(s) on 429, 503.
    pub async fn everything_messages(
        &self,
        params: &GetEverythingMessagesParams,
    ) -> Result<Page<GetEverythingMessagesResponseContent>, Error> {
        let mut operation = self.client.operation(&routes::GET_EVERYTHING_MESSAGES, &[]);
        operation.query_optional("page", params.page.as_ref());
        self.client.send_page(operation).await
    }

    /// Open cards with no due date across all accessible projects, grouped by project (paginated).
    ///
    /// `GET /cards/no_due_date.json` — idempotent; retries up to 3 attempt(s) on 429, 503.
    pub async fn everything_no_due_date_cards(
        &self,
        params: &GetEverythingNoDueDateCardsParams,
    ) -> Result<Page<GetEverythingNoDueDateCardsResponseContent>, Error> {
        let mut operation = self
            .client
            .operation(&routes::GET_EVERYTHING_NO_DUE_DATE_CARDS, &[]);
        operation.query_list_optional("assignee_ids[]", params.assignee_ids.as_deref());
        operation.query_optional("due", params.due.as_ref());
        operation.query_optional("page", params.page.as_ref());
        self.client.send_page(operation).await
    }

    /// Open to-dos with no due date across all accessible projects, grouped by project (paginated).
    ///
    /// `GET /todos/no_due_date.json` — idempotent; retries up to 3 attempt(s) on 429, 503.
    pub async fn everything_no_due_date_todos(
        &self,
        params: &GetEverythingNoDueDateTodosParams,
    ) -> Result<Page<GetEverythingNoDueDateTodosResponseContent>, Error> {
        let mut operation = self
            .client
            .operation(&routes::GET_EVERYTHING_NO_DUE_DATE_TODOS, &[]);
        operation.query_list_optional("assignee_ids[]", params.assignee_ids.as_deref());
        operation.query_optional("due", params.due.as_ref());
        operation.query_optional("page", params.page.as_ref());
        self.client.send_page(operation).await
    }

    /// Cards parked in a project's "Not now" column across all accessible projects, grouped by project (paginated).
    ///
    /// `GET /cards/not_now.json` — idempotent; retries up to 3 attempt(s) on 429, 503.
    pub async fn everything_not_now_cards(
        &self,
        params: &GetEverythingNotNowCardsParams,
    ) -> Result<Page<GetEverythingNotNowCardsResponseContent>, Error> {
        let mut operation = self
            .client
            .operation(&routes::GET_EVERYTHING_NOT_NOW_CARDS, &[]);
        operation.query_list_optional("assignee_ids[]", params.assignee_ids.as_deref());
        operation.query_optional("due", params.due.as_ref());
        operation.query_optional("page", params.page.as_ref());
        self.client.send_page(operation).await
    }

    /// Incomplete cards in active columns across all accessible projects, grouped by project (paginated).
    /// Each bucket entry carries the matching cards and their steps.
    ///
    /// `GET /cards/open.json` — idempotent; retries up to 3 attempt(s) on 429, 503.
    pub async fn everything_open_cards(
        &self,
        params: &GetEverythingOpenCardsParams,
    ) -> Result<Page<GetEverythingOpenCardsResponseContent>, Error> {
        let mut operation = self
            .client
            .operation(&routes::GET_EVERYTHING_OPEN_CARDS, &[]);
        operation.query_list_optional("assignee_ids[]", params.assignee_ids.as_deref());
        operation.query_optional("due", params.due.as_ref());
        operation.query_optional("page", params.page.as_ref());
        self.client.send_page(operation).await
    }

    /// Active, incomplete to-dos across all accessible projects, grouped by project (paginated).
    /// Each bucket entry carries the matching to-dos and their steps.
    ///
    /// `GET /todos/open.json` — idempotent; retries up to 3 attempt(s) on 429, 503.
    pub async fn everything_open_todos(
        &self,
        params: &GetEverythingOpenTodosParams,
    ) -> Result<Page<GetEverythingOpenTodosResponseContent>, Error> {
        let mut operation = self
            .client
            .operation(&routes::GET_EVERYTHING_OPEN_TODOS, &[]);
        operation.query_list_optional("assignee_ids[]", params.assignee_ids.as_deref());
        operation.query_optional("due", params.due.as_ref());
        operation.query_optional("page", params.page.as_ref());
        self.client.send_page(operation).await
    }

    /// Get every overdue card across all accessible projects, oldest-due-date-first.
    /// A complete, unpaginated array; each item embeds its `bucket`.
    ///
    /// `GET /cards/overdue.json` — idempotent; retries up to 3 attempt(s) on 429, 503.
    pub async fn everything_overdue_cards(
        &self,
        params: &GetEverythingOverdueCardsParams,
    ) -> Result<GetEverythingOverdueCardsResponseContent, Error> {
        let mut operation = self
            .client
            .operation(&routes::GET_EVERYTHING_OVERDUE_CARDS, &[]);
        operation.query_list_optional("assignee_ids[]", params.assignee_ids.as_deref());
        operation.query_optional("due", params.due.as_ref());
        self.client.send(operation).await
    }

    /// Get every overdue to-do across all accessible projects, oldest-due-date-first.
    /// A complete, unpaginated array; each item embeds its `bucket`.
    ///
    /// `GET /todos/overdue.json` — idempotent; retries up to 3 attempt(s) on 429, 503.
    pub async fn everything_overdue_todos(
        &self,
        params: &GetEverythingOverdueTodosParams,
    ) -> Result<GetEverythingOverdueTodosResponseContent, Error> {
        let mut operation = self
            .client
            .operation(&routes::GET_EVERYTHING_OVERDUE_TODOS, &[]);
        operation.query_list_optional("assignee_ids[]", params.assignee_ids.as_deref());
        operation.query_optional("due", params.due.as_ref());
        self.client.send(operation).await
    }

    /// Open, unassigned cards across all accessible projects, grouped by project (paginated).
    ///
    /// `GET /cards/unassigned.json` — idempotent; retries up to 3 attempt(s) on 429, 503.
    pub async fn everything_unassigned_cards(
        &self,
        params: &GetEverythingUnassignedCardsParams,
    ) -> Result<Page<GetEverythingUnassignedCardsResponseContent>, Error> {
        let mut operation = self
            .client
            .operation(&routes::GET_EVERYTHING_UNASSIGNED_CARDS, &[]);
        operation.query_list_optional("assignee_ids[]", params.assignee_ids.as_deref());
        operation.query_optional("due", params.due.as_ref());
        operation.query_optional("page", params.page.as_ref());
        self.client.send_page(operation).await
    }

    /// Open, unassigned to-dos across all accessible projects, grouped by project (paginated).
    ///
    /// `GET /todos/unassigned.json` — idempotent; retries up to 3 attempt(s) on 429, 503.
    pub async fn everything_unassigned_todos(
        &self,
        params: &GetEverythingUnassignedTodosParams,
    ) -> Result<Page<GetEverythingUnassignedTodosResponseContent>, Error> {
        let mut operation = self
            .client
            .operation(&routes::GET_EVERYTHING_UNASSIGNED_TODOS, &[]);
        operation.query_list_optional("assignee_ids[]", params.assignee_ids.as_deref());
        operation.query_optional("due", params.due.as_ref());
        operation.query_optional("page", params.page.as_ref());
        self.client.send_page(operation).await
    }
}
