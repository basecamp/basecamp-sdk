# SPEC §19 Zero-Skip roster — the source of truth for what each conformance
# runner does not execute.
#
# Purpose: SPEC §19 claims to enumerate "every skip a default (mock-mode)
# conformance run reports, one line per runner × test, verbatim from the
# runners' skip mechanisms". That roster used to live in SPEC.md as prose and be
# READ BACK OUT of it by a parser. It lives here instead, and SPEC's block is
# now RENDERED from this file — the prose is an output, not an input.
#
# What enforces it, in two halves that answer different questions:
#
#   scripts/sync-doc-constants.rb (`make doc-constants-check`) renders the
#   <!-- @zero-skip-roster:begin/end --> block in SPEC.md from this file and
#   requires the text between those markers to be BYTE-IDENTICAL. `--write`
#   (reached from `make generate` via sync-api-version) rewrites it. Nothing
#   parses the block, so nothing can misread it: a character SPEC carries that
#   this file does not produce is a diff, whatever it is made of.
#
#   scripts/check-fixture-execution.rb (`make check-fixture-execution`) compares
#   the `skips` below, per runner, for set equality against the execution
#   manifests the runners write (conformance/manifests/<runner>.json). A skip
#   added to a runner without an entry here, or an entry left behind after a gap
#   closes, fails the build. It needs a conformance run; the block check does
#   not, which is why the two live in different gates.
#
# The CLASSIFICATION and the reasons stay judgement — nothing asserts them, and
# that half is why §19 keeps its `[manual]` tag.
#
# --- Entry semantics ----------------------------------------------------------
#
# `runners` holds exactly seven keys — go, python, ruby, typescript, kotlin,
# swift, rust — and every one is required. A runner is not omitted when it skips
# nothing; it carries an empty `skips` list and says why. An absent key would
# read as "not stated yet" and contribute nothing to either comparison, which is
# how a runner's skips go unrecorded.
#
# A key written TWICE is refused at every level — a repeated runner section, a
# repeated field, a repeated `case`. YAML allows it and keeps only the last, so
# the earlier claim would be discarded in silence: written here, rendered
# nowhere, compared against nothing. That is the same false green this file
# replaced a parser to retire, and it is checked against the parsed document
# rather than the loaded Hash, which no longer knows the duplicate happened.
#
# The same reasoning runs through every other rule the loader enforces, and they
# are one failure in several costumes rather than a list to memorise. This file
# is ONE YAML document; `runners` is its ONLY top-level key; merge keys (`<<`)
# are refused alongside aliases; and no value may contain a control character, a
# Unicode line or paragraph separator, or `<` or `&`.
#
# Text after a second `---`, text under a misspelled top-level key, and entries
# merged in by a `<<` that an explicit field then overrides are each a claim
# written here and read by nothing — which is precisely how the prose reader used
# to ignore a second roster block. A bare CR or U+2028 inside a quoted scalar is
# the same thing from the other side: it renders a line the roster never stated,
# and the block still matches byte for byte. `<` and `&` are the sharpest,
# because they are markup rather than text: they open an HTML comment or element
# that hides everything after it, or inject a marker the writer emits and the
# next check refuses. A value that wants one must be REPHRASED: the rule reads
# the scalar rather than the rendered Markdown, so backticks around the
# character do not exempt it.
#
# The character rules are written as CLASSES rather than lists of spellings, so
# there is nothing to add when the next one is thought of.
#
#   source          Required. The runner file and skip constant, rendered
#                   verbatim inside the parentheses of the heading. Kotlin's and
#                   Swift's carry a clause rather than a bare constant name,
#                   because one of their entries does not come from the
#                   constant at all: it arrives from the `link-header` tag
#                   branch, which no name-keyed entry can express. That
#                   distinction is load-bearing documentation: a reader who
#                   greps `KOTLIN_SKIPS` for that case finds nothing and must
#                   not conclude the roster is stale. (The constants themselves
#                   hold the Go-first composite's cases — every case in
#                   `conformance/tests/recording_summary.json`; a port empties
#                   them again.)
#
#   classification  Optional. One word for why the skips exist at all
#                   (`architectural`); omitted where the reasons carry it. A
#                   clause, like `note` below, and held to the same rule.
#
#   note            Optional. A clause, NOT a sentence: the renderer supplies
#                   the terminating punctuation, so write it without one. This
#                   is ENFORCED, not merely asked for — a qualifier ending in
#                   `. ! ? ; : ,` is refused by the loader, because
#                   `nothing to skip.` renders `nothing to skip..` and no gate
#                   downstream can object to prose the renderer itself produced.
#
#   skips           Required, possibly empty. Each entry is
#                   { case: <exact case name>, reason: <complete sentence> }.
#                   `case` must match the fixture case name byte for byte — it
#                   is what the manifest comparison keys on. `reason` is
#                   rendered verbatim after an em dash, so it carries its own
#                   final period.
#
# An empty `skips` renders as "— none" in the heading. That word is DERIVED, not
# written: a section cannot claim to skip nothing while listing skips, and one
# that skips nothing cannot forget to say so.
#
# --- Rendering ----------------------------------------------------------------
#
# One section per runner, in the order of scripts/zero_skip_roster.rb's RUNNERS,
# which is the order SPEC has always used. This file's key order is irrelevant
# to the output; do not rely on it.
#
#   **<Label>** (<source>)[ — <qualifiers joined by "; ">]<":" or ".">
#   - "<case>" — <reason>
#
# where the qualifiers are, in order: `none` when there are no skips,
# `classification`, `note`. The terminator is ":" when skips follow and "." when
# none do.
#
# Lines are not wrapped. A wrapped heading would be a second rendering rule to
# get byte-identical, and the bullets have never been wrapped either.

runners:
  go:
    source: "`conformance/runner/go/main.go` `goSDKSkips`"
    classification: architectural
    note: "same-origin logic is covered by `TestIsSameOrigin` unit tests"
    skips:
      - case: "Mixed-case host and explicit default port stay on the mocked origin"
        reason: "Go runner dials `configOverrides.baseUrl` directly; its `httptest` mock owns its origin, so origin-interception normalization does not apply."
      - case: "Bracketed IPv6 loopback origin stays on the mocked origin"
        reason: "same as above."

  # Python's `SKIPS` is empty: the Go-first composite is ported here, so its
  # cases run. The `link-header` fixture RUNS too — only its `requestCount`
  # assertion is suppressed — so it is not a skip and must not be rostered as
  # one.
  python:
    source: "`conformance/runner/python/runner.py` `SKIPS`"
    note: "the `link-header` fixture above runs; only its `requestCount` assertion is suppressed"
    skips: []

  ruby:
    source: "`conformance/runner/ruby/runner.rb` `RUBY_SKIPS`"
    skips:
      - case: "PUT operation is naturally idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "DELETE operation is naturally idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "POST operation retries when marked idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "Subscribe POST retries when marked idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "CreateBookmark POST retries when marked idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "DeleteBookmark DELETE retries when marked idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "CreateBubbleUp POST retries when marked idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "DeleteBubbleUp DELETE retries when marked idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "SpotlightRecording POST retries when marked idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "UnspotlightRecording DELETE retries when marked idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "RecordProjectVisit POST retries when marked idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "UpdateProjectClientAccess PUT retries when marked idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "EnableProjectClients POST retries when marked idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "DisableProjectClients DELETE retries when marked idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "UpdateMyNote PUT retries when marked idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "UpdateCalendar PUT retries when marked idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "PrioritizeAssignment POST retries when marked idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "DeprioritizeAssignment DELETE retries when marked idempotent"
        reason: "GET-only retry (waiver 2B.3)."
      - case: "Network error on an idempotent POST is retried then succeeds"
        reason: "GET-only network retry (waiver 2B.3)."
      - case: "RecordingsSummarize routes comment.created to the comment read and reads its mentions"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize refuses boost.created before any request"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize finds a chat line under the second visible Campfire"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize reports a chat line under no visible Campfire as unresolved, not as a failed read"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize returns a Campfire candidate's 403 as that read's error"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "CommentsCreateWithMentions resolves each person before posting and writes the mention from attachable_sgid"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes message.created (event type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes todo.created (event type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes card.created (event type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Document (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Upload (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Schedule::Entry (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Question (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Question::Answer (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Todolist (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Vault (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Inbox::Forward (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Client::Approval (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Client::Correspondence (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes GoogleDocument (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes CloudFile (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Kanban::Step (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Questionnaire (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Schedule (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Todoset (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Message::Board (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Kanban::Board (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Kanban::Column (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Inbox (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes todo.completed (event type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes todo.assignment_changed (event type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes card.completed (event type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes card.assignment_changed (event type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Comment (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Message (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Todo (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Kanban::Card (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Chat::Transcript (recording type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes card.moved (event type) to one typed read and projects the recording"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."
      - case: "RecordingsSummarize routes Chat::Lines::Text (recording type) through Campfire discovery and projects the line"
        reason: "unwaivered, tracked work: a Go-first composite recorded in SPEC Appendix F (Recording Summaries and Mention Helpers); the Go runner executes it, this SDK has no port yet, and a port deletes this entry."

  typescript:
    source: "`conformance/runner/typescript/runner.test.ts` `TS_SDK_SKIPS`"
    skips:
      - case: "Large integer IDs preserved without precision loss"
        reason: "`Number` is 53-bit (waiver 1B.6)."

  # No section-level classification: the two kinds of entry below differ in
  # status and must not share a label. The `link-header` case is architectural
  # and says so in its reason; every `recording_summary.json` case — the
  # Go-first composite's whole fixture — is unwaivered tracked work (SPEC
  # Appendix F), and each reason says that.
  kotlin:
    source: "`kotlin/conformance/.../Main.kt` — the entry below comes from the `link-header` tag branch, not from `KOTLIN_SKIPS`, which the port of the Go-first composite emptied"
    skips:
      - case: "List operation returns first page with Link header"
        reason: "architectural: the SDK auto-paginates, and its status model reports the last consumed response, so a one-response queue yields \"no response\" (see the tag-branch discussion above)."

  swift:
    source: "`conformance/runner/swift/.../Runner.swift` — `temporarySkips` is empty since the Go-first composite was ported; the one entry below comes from the `link-header` tag branch"
    skips:
      - case: "List operation returns first page with Link header"
        reason: "architectural, same as Kotlin: auto-pagination plus a last-consumed-response status model."

  # Rust's `RUST_SKIPS` is empty. Its runner drives the SDK through a scripted
  # `HttpClient` rather than a socket, so the two origin-normalization cases Go
  # cannot dial run here; the `link-header` fixture RUNS with only its
  # `requestCount` assertion suppressed; and the Go-first composite is ported,
  # so its cases run too.
  rust:
    source: "`conformance/runner/rust/src/main.rs` `RUST_SKIPS`"
    note: "the `link-header` fixture above runs; only its `requestCount` assertion is suppressed"
    skips: []
